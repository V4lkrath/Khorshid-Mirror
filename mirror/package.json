{
  "name": "Khorshid-mirror",
  "version": "0.1.0",
  "private": true,
  "description": "Khorshid's mirror scraper. Runs in GitHub Actions every ~5 min, writes channel snapshots + media to the export branch.",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "scrape": "tsx scrape.ts",
    "dry-run": "tsx dry-run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}